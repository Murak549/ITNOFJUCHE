VIN_independent_war = {
    VIN_definitive_victory = {
        activation = {
            AND = {
                NOT = {
                    has_global_flag = VIN_INDEPENDENT
                }
                286 = {
                    is_fully_controlled_by = VIN
                }
                671 = {
                    is_fully_controlled_by = VIN
                }
                has_war = yes
            }
        }
        available = {
            has_war = no
            # 286 = {
            #     is_fully_controlled_by = VIN
            # }
            # 671 = {
            #     is_fully_controlled_by = VIN
            # }
        }
        cancel_trigger = {
            has_war = no
        }
        icon = GFX_decision_category_marching_soldiers
        is_good = yes
        days_mission_timeout = 140
        selectable_mission = no
        timeout_effect = {
            VIN = {
                every_country = {
                    limit = {
                        has_war_with = VIN 
                    }
                    white_peace = VIN
                }
            }
            white_peace = FRA
            FRA = {
                annex_country = {
                    target = IDC
                    transfer_troops = no
                }
                release = LAO
                release = CAM
            }
            286 = {
                add_core_of = SVN
                transfer_state_to = SVN
            }
            SVN = {
                set_cosmetic_tag = VIN
            }
            set_global_flag = VIN_INDEPENDENT
        }
    }
    VIN_sex_with_stalin = {
        available  = {
				has_completed_focus  = CHI_bureau_of_investigation_and_statistics 
			}
    }
}